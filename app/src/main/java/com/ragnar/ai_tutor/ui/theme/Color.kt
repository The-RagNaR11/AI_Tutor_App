package com.ragnar.ai_tutor.ui.theme

import androidx.compose.ui.graphics.Color


val Black = Color(0xFF000000)
val White = Color(0xFFFFFFFF)

// Brand & Accent Colors
val BrandPrimary = Color(0xFF5B56D7) // Used for avatar, send button, icons, audio player
val AccentGreen = Color(0xFF1ED760)  // User chat bubble background

// Background Colors
val BackgroundPrimary = Color(0xFFEAEAEA) // Main screen background
val BackgroundSecondary = Color(0xFFC7C7C7) // Card and text input background
val AiMessageBackground = Color(0xFFECECEC)  // AI chat bubble background
val ChipBackground = Color(0xFFE8E7FA)      // "Current" status chip background

// Text Colors
val TextPrimary = Color(0xFF1F1F1F)       // For main headings and important text
val TextSecondary = Color(0xFF8A8A8E)     // For subtitles, timestamps, and placeholder text
val TextOnPrimary = Color(0xFFFFFFFF)     // Text on dark/colored backgrounds (e.g., send button)
val TextOnAccent = Color(0xFFFFFFFF)      // Text on the green user chat bubble

// Icon Colors
val IconPrimary = Color(0xFF5B56D7)       // Active or primary icons
val IconSecondary = Color(0xFF8A8A8E)     // Inactive or secondary icons (e.g., calendar, clock)

// UI Component-Specific Colors
val WaveformActive = Color(0xFF5B56D7)
val WaveformInactive = Color(0xFFD9D9D9)

// UI States
val ColorHint = Color(0xFF808080)
val ColorError = Color(0xFFFF3B30)
val ColorSuccess = Color(0xFF34C759)
val ColorWarning = Color(0xFFFF9500)